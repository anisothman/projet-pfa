[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "projet-pfa"
version = "0.1.0"
description = "Mon projet PFA"
requires-python = ">=3.8"
dependencies = [
    "google-search-results",
]

[tool.setuptools.packages.find]
where = ["src"]